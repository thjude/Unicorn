using UnityEngine;
using System.Collections.Generic;

public class ScreenSelectorScript : MonoBehaviour {
    public List<GameObject> Selectors;

    // Use this for initialization
    private void Start() {
        /*var selectors = GetComponentsInChildren<GameObject>();
        foreach (GameObject s in selectors) {
            _nbSelector++;
            Selectors.Add(s);
        }*/
    }
}